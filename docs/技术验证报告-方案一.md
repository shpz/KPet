# 方案一技术验证（Spike）报告

> 日期：2026-07-31；环境：UE 5.7（Epic 启动器安装版）/ Win11 / VS2022 Community。
> 验证工程：`spike/KimiPetSpike/`（C++，可重复构建运行，命令见文末）。
> 对应设计文档：§5.3（方案一：离屏渲染 + 自建透明窗）、§9 第 1 步。

## 结论

**方案一管线全部验证通过（8/8）。** 初验时的阻断项（"alpha 反转"）经源码取证确认为 UE 的**文档化语义**而非引擎 bug，CPU 侧一行转换即可修正。启动器安装版引擎开箱可用，无需改引擎源码。

> 验证分两轮：首轮在 UE 5.7 上完成 1-7 项并定位 alpha 问题；修复后在 **UE 5.8**（目标版本）上复测全部通过。

## 验证结果明细

| # | 验证项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 场景捕获 → 回读 → 分层窗口 上屏管线 | ✅ | 持续 10 万+ 帧无中断（日志 `Presented N frames`） |
| 2 | 游戏窗 `ShowWindow(SW_HIDE)` 后捕获持续更新 | ✅ | 隐藏后帧流不断（§5.3 关键假设成立，无需 `-RenderOffscreen`） |
| 3 | 异步回读时序（取上一帧） | ✅ | 无卡顿、无撕裂，单槽流水线足够 |
| 4 | 分层窗口样式（置顶/无任务栏/不抢焦点） | ✅ | 窗口行为符合预期 |
| 5 | 不透明像素拖拽窗口（超系统阈值） | ✅ | 用户实测：拖球体窗口跟随移动 |
| 6 | 透明像素鼠标穿透 | ✅ | 用户实测：透明区点击穿透到桌面（分层窗口文档化行为） |
| 7 | 帧率 / 回读带宽 | ✅ | 320×320 隐藏窗满速 ~125 FPS（预算 30），回读无瓶颈 |
| 8 | RT 逐像素透明正确性（背景 a=0 / 内容 a=255） | ✅（修复后） | 见下 |

## 关键发现：RT 像素语义（§5.3"待实测"项的答案）

**UE 的 scene capture 输出像素是「RGB 已按不透明度预乘 + alpha 存反向不透明度（1 − opacity）」**，不是常规直觉的「直线 RGB + 不透明度 alpha」：

- 背景：RGB≈0，alpha=255（反向：不透明度 0）
- 不透明球体：RGB=颜色，alpha=0（反向：不透明度 255）

源码证据（UE 5.4 `EngineTypes.h:493`，语义沿用至 5.8 实测一致）：

```
SCS_SceneColorHDR UMETA(DisplayName="SceneColor (HDR) in RGB, Inv Opacity in A")
```

`r.PostProcessing.PropagateAlpha`（5.6+ 改名 **Alpha Output**，bool）打开后，这个"Inv Opacity"语义被原样带过 tonemapper 进入 FinalColorLDR 输出。**修复只需 CPU 侧：`A = 255 - A_read`，RGB 已是预乘直接拷贝（不要二次预乘）。** 修复后实测：背景 alpha=0 占 46.4%、内容 alpha=255、AA 边缘 ~630 个中间值——完美的逐像素透明梯度。

> 推论：设计文档 §5.3 提到的"CPU 侧转预乘"步骤在真实语义下**不需要**——引擎输出的就是预乘结果，反而要避免二次预乘。

## 排查/迭代期间确认的工程要点

- `r.PostProcessing.PropagateAlpha` 在 5.6+ 为 bool（旧值 `2` 自动转换并回写 ini），即项目设置里的 **Alpha Output** 开关；本管线**必须打开**。
- 该语义下 BasePass 不写 alpha，透明度信息在场景合成（ComposeSeparateTranslucency 等）阶段产生；`ShowFlags.PostProcessing=false` 会得到全 0 alpha（无意义），不能这么用。
- `BuildSettingsVersion.V5` 与共享引擎产物冲突 → Target.cs 用 `Latest`。
- `TUniquePtr<前置声明类型>` 成员被 UHT 生成代码实例化析构 → 分层窗口成员改裸指针手动管理。
- `t.IdleWhenNotForeground` 是 cheat 级 cvar，写 ini 会 ensure，且实测不需要（隐藏窗仍满速渲染）。
- 灯光 `CastShadows` / 球体 `CastShadow` 关闭（无接收面，阴影只污染 alpha）；capture 的 Fog/VolumetricFog/Cloud/SkyLighting showflags 关闭（保证背景纯 a=0）。
- 材质 MID 的父材质要用 `/Engine/BasicShapes/BasicShapeMaterial_Inst`；直接用 `BasicShapeMaterial` 创建 MID 会丢参数映射，Color 不生效。
- `ConstructorHelpers::FObjectFinder` 只能在构造函数用（BeginPlay 里用会 fatal）。
- 迭代前先 `taskkill` 残留 UnrealEditor.exe，否则链接器因 DLL 占用失败。

## 对设计文档的修订建议

- §5.3 实现设计补一条："RT 像素为预乘 RGB + 反向 alpha（Inv Opacity），CPU 侧取反 alpha 后直接用于 UpdateLayeredWindow，无需预乘步骤"。
- §5.1 的 5.8 复核：已通过本 spike 在 5.8 实测完成（管线行为与 5.7 一致）。
- R1 风险可降为「已解决」：方案一各环节验证通过。

## 复现

```bash
# 构建（UE 5.8）
"/c/Program Files/Epic Games/UE_5.8/Engine/Build/BatchFiles/Build.bat" KimiPetSpikeEditor Win64 Development \
  -Project="D:/Workspace/KimiPet/spike/KimiPetSpike/KimiPetSpike.uproject" -WaitMutex
# 运行（先杀残留进程）
taskkill //F //IM UnrealEditor.exe
"/c/Program Files/Epic Games/UE_5.8/Engine/Binaries/Win64/UnrealEditor.exe" \
  "D:/Workspace/KimiPet/spike/KimiPetSpike/KimiPetSpike.uproject" -game -windowed -ResX=320 -ResY=240 -log -stdout -unattended
```

- 日志：`spike/KimiPetSpike/Saved/Logs/KimiPetSpike.log`（每 60 帧 alpha 分布统计）
- 第 300 帧原始 RT 像素落盘：`spike/KimiPetSpike/Saved/Diag/rt_{r,g,b,a}.ppm`（PPM 文本，可脚本分析）
- 诊断代码：`LayeredPetWindow.cpp`（落盘 + 统计）、`SpikeCaptureActor.cpp`（`DiagBall` 对照球）
