# 构建产物（node_modules 与 dist-plugin/ 由仓库根 .gitignore 统一忽略；package-lock.json 需入库以保证依赖可复现）
dist/
dist-test/
bin/
# bin/ 仅忽略构建产物（bin/kpetd.exe）；packaging 下的源码脚本需入库（WSL relay 包装，跨平台兼容方案形态一）
!packaging/kpet/bin/
!packaging/kpet/bin/kpet-relay.sh
